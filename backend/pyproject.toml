[project]
name = "inventorymgr-api"
version = "0.1.0"
description = "InventoryMGR FastAPI backend"
requires-python = ">=3.13"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "sqlalchemy>=2",
  "alembic",
  "psycopg[binary]",
  "pydantic-settings>=2.14.2",
  "PyJWT",
  "pwdlib[argon2]",
  "python-multipart",
  "email-validator",
  "slowapi>=0.1.10",
  "xlsxwriter",
  "ldap3",
  "cryptography",
]

[project.optional-dependencies]
dev = [
  "pytest",
  "pytest-asyncio",
  "httpx",
  "ruff",
  "mypy",
]

[tool.uv]
package = false

[tool.ruff]
target-version = "py313"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501", "B904", "E712"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.mypy]
python_version = "3.13"
plugins = []
ignore_missing_imports = true
strict = false
