---
# Run E2E tests in Docker: docker compose -f docker-compose.e2e.yml run --rm playwright
services:
  db-test:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: inventorymgr
      POSTGRES_PASSWORD: password
      POSTGRES_DB: inventorymgr_test
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U inventorymgr -d inventorymgr_test"]
      interval: 5s
      timeout: 5s
      retries: 20

  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    environment:
      APP_ENV: test
      TEST_DATABASE_URL: "postgresql+psycopg://inventorymgr:password@db-test:5432/inventorymgr_test"
      DATABASE_URL: "postgresql+psycopg://inventorymgr:password@db-test:5432/inventorymgr_test"
      JWT_SECRET: "e2e-test-only-not-for-production"
    depends_on:
      db-test:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/health"]
      interval: 10s
      timeout: 5s
      retries: 6

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
      args:
        INVENTORYMGR_API_URL: http://backend:8000
    environment:
      INVENTORYMGR_API_URL: http://backend:8000
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/"]
      interval: 10s
      timeout: 5s
      retries: 6

  playwright:
    image: mcr.microsoft.com/playwright:v1.60.0-jammy
    user: root
    working_dir: /app
    volumes:
      - ./frontend:/app:z
      - playwright-node-modules:/app/node_modules
    environment:
      BASE_URL: http://frontend:3000
      PLAYWRIGHT_BROWSERS_PATH: /ms-playwright
    depends_on:
      frontend:
        condition: service_healthy
    command: >
      bash -c "npm install --legacy-peer-deps --silent --no-save && npx playwright test --project=chromium"

volumes:
  playwright-node-modules:
